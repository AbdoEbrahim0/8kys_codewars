#include<string>
using namespace std;

string bonus_time(int salary, bool bonus)
{
//return "$" + to_string(salary * (bonus ? 10 : 1)); 
string s=bonus?  to_string(salary*10):to_string (salary);
    return "$" + s;

}
