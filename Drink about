#include <string>

std::string people_with_age_drink(int age) {
  if (age <14 && age>=0) return "drink toddy";
  else if (age <18 && age>=14) return "drink coke";
  else if (age <21 && age>=18) return "drink beer";
  if (age>=21) return "drink whisky";
   
}
