#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <cassert>
#include <string>
#include <algorithm>
#include <cstring>
#include <stdio.h>
#include <vector>
#include <regex>
#include <string>
#include <numeric>
#include <stdlib.h>
#include <map>
#include <numeric>
#include<sstream>
using namespace std;
    //method 1:
// int arrayPlusArray(std::vector<int> a, std::vector<int> b) {
//   int sum=0;
//   for (int i=0;i<a.size();i++)
//     {
//   sum=a[i] +sum; 
//   }
//   for (int j=0;j<b.size();j++)
//     {
//   sum=b[j]+sum; 
//   }  
//   return sum; // something went wrong
// }
    //method 2:
int arrayPlusArray(std::vector<int> a, std::vector<int> b) {  
  return accumulate(a.begin(),a.end(),0) +accumulate(b.begin(),b.end(),0); // something went wrong
}
    //method 3:

    //method 4:


int main() {
  // int arr[]={1, 2, 3}, arr2[]={4, 5, 6};
  //methode 1:
//   string x ="abdo magdy";
//  cout<<makeUpperCase(x)<<"\n";
 //methode 2 and  3 and 4 and 5::
 
//  cout<<switch_it_up(10)<<"\n";
vector<int>vec={1, 2, 3},vec2={4, 5, 6};
cout<<arrayPlusArray(vec,vec2)<<"\n";    

return 0;
}
